const eris = require("eris");
module.exports = {
    byEris: eris.Constants,
    perms: {
        sendMessages: "sendMessages",
        readMessages: "readMessages",
        embedLinks: "embedLinks"
    }
}